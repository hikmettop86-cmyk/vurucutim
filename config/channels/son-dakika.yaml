slug: son-dakika
name: "Son Dakika"
keywords: ["son dakika", "deprem", "faiz", "dolar", "seçim", "asgari ücret"]
rss_locale: "hl=tr&gl=TR&ceid=TR:tr"
schedule_cron: "0 2,6,10,14,18,22 * * *"
duration_s: 6
min_score: 6.0
max_candidates_per_run: 10
template: default
colors:
  primary: "#c81e1e"
  accent: "#ffea3b"
  bg_gradient: ["#1a3b6b", "#0a1a3b"]
handle: "@HaberShortsTR"
output_dir: output/son-dakika
enabled: true
cta:
  # End-screen CTA is disabled — persistent BEĞEN/ABONE chips are shown throughout
  # the video instead, so the viewer never gets a "video is ending" signal.
  enabled: false
  text: "BEĞEN · ABONE OL · PAYLAŞ"
  icons: ["❤️", "🔔", "↗️"]
  duration_s: 3
  show_handle: true
