# Web Panel Implementation Plan (Phase 3)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a local Flask + HTMX web panel for managing channels (creation wizard with Opus DNA + visual DNA editor with live preview), watching produced Shorts, monitoring RSS scoring, viewing logs, and triggering manual runs. APScheduler runs cron jobs in-process.

**Architecture:** Flask app factory + 8 blueprints + Flask-SQLAlchemy ORM (mapped onto Phase 1's existing SQLite schema) + APScheduler `BackgroundScheduler` + threaded pipeline runners + per-channel filelock (already in pipeline). Frontend: Tailwind CDN + HTMX 2.x + Alpine.js (no build step). Live template preview via a `/preview/<slug>` endpoint that returns `renderer.build_html()` directly — browser renders, ~50ms latency.

**Tech Stack:** Flask 3, Flask-SQLAlchemy 3, APScheduler 3, Flask-WTF (CSRF), Tailwind CDN, HTMX 2.x, Alpine.js. Phase 1+2 code (renderer, dna, config, locale, pipeline, etc.) is reused unchanged.

**Spec reference:** `docs/superpowers/specs/2026-05-05-web-panel-design.md`

**Phase 1+2 status:** complete (`v0.2.0-channel-dna` tag, 150 tests). This plan adds on top.

---

## File Structure

```
src/short_bot/
├── cli.py                          # +'web' subcommand
└── web/                            # NEW package
    ├── __init__.py                 # Flask app factory + scheduler init
    ├── extensions.py               # db = SQLAlchemy()
    ├── models.py                   # ORM models (Short, Run, RssItem, ProcessedItem)
    ├── runs.py                     # Threaded pipeline runner + helpers
    ├── scheduler.py                # APScheduler wiring
    ├── routes/
    │   ├── __init__.py             # register_blueprints helper
    │   ├── dashboard.py            # /
    │   ├── shorts.py               # /shorts, /shorts/<id>, +HTMX, +actions
    │   ├── rss.py                  # /rss, /rss/<id>/produce
    │   ├── channels.py             # /channels (list)
    │   ├── channel_new.py          # /channels/new wizard
    │   ├── channel_edit.py         # /channels/<slug>/edit
    │   ├── preview.py              # /preview/<slug>
    │   └── logs.py                 # /logs, /logs/tail
    ├── templates/
    │   ├── base.html.j2            # nav + Tailwind + HTMX + Alpine
    │   ├── dashboard.html.j2
    │   ├── shorts/
    │   │   ├── list.html.j2
    │   │   └── detail.html.j2
    │   ├── channels/
    │   │   ├── list.html.j2
    │   │   ├── new.html.j2
    │   │   └── edit.html.j2
    │   ├── rss.html.j2
    │   ├── logs.html.j2
    │   └── _partials/
    │       ├── shorts_grid.html.j2
    │       ├── run_status.html.j2
    │       ├── dna_preview.html.j2
    │       ├── log_tail.html.j2
    │       └── toast.html.j2
    └── static/
        └── custom.css              # tiny, tailwind covers most

config/settings.yaml                # web.port: 5000 → 5005 (default)
pyproject.toml                      # +flask, flask-sqlalchemy, apscheduler, flask-wtf

tests/
├── fixtures/
│   ├── sample_script_tr.json       # generic news sample, 5 languages
│   ├── sample_script_en.json
│   ├── sample_script_de.json
│   ├── sample_script_es.json
│   └── sample_script_fr.json
├── test_web_app_factory.py
├── test_web_dashboard.py
├── test_web_shorts.py
├── test_web_rss.py
├── test_web_channels.py
├── test_web_channel_new.py
├── test_web_preview.py
├── test_web_logs.py
├── test_web_runs.py
├── test_web_scheduler.py
└── test_web_models.py
```

**Total: 26 tasks across 5 phases.**

---

## Task 1: pyproject deps + settings.yaml port

**Files:**
- Modify: `pyproject.toml`
- Modify: `config/settings.yaml`
- Modify: `src/short_bot/config.py` (Settings.web_port default)

- [ ] **Step 1: Update `pyproject.toml`**

Find the `[project] dependencies` block and append:

```python
    "flask>=3.0",
    "flask-sqlalchemy>=3.1",
    "apscheduler>=3.10",
    "flask-wtf>=1.2",
```

- [ ] **Step 2: Update `config/settings.yaml`**

Find the `web:` block:

```yaml
web:
  host: "127.0.0.1"
  port: 5000
```

Replace `port: 5000` with `port: 5005`.

- [ ] **Step 3: Update `Settings.web_port` default in `src/short_bot/config.py`**

Find `web_port=int(web.get("port", 5000))` and replace with:

```python
        web_port=int(web.get("port", 5005)),
```

- [ ] **Step 4: Install deps**

```bash
pip install -e ".[dev]"
```

Expected: installs flask, flask-sqlalchemy, apscheduler, flask-wtf without error.

- [ ] **Step 5: Run all tests**

```bash
python -m pytest -v
```

Expected: 150 still passing (no regression).

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml config/settings.yaml src/short_bot/config.py
git commit -m "chore(deps): flask + flask-sqlalchemy + apscheduler + flask-wtf; default port 5005"
```

---

## Task 2: SQLAlchemy ORM models

**Files:**
- Create: `src/short_bot/web/__init__.py` (empty placeholder so package importable)
- Create: `src/short_bot/web/extensions.py`
- Create: `src/short_bot/web/models.py`
- Test: `tests/test_web_models.py`

- [ ] **Step 1: Create empty package init**

`src/short_bot/web/__init__.py`:
```python
"""short-bot web panel package."""
```

- [ ] **Step 2: Create `extensions.py`**

```python
"""Flask extension singletons."""
from flask_sqlalchemy import SQLAlchemy

db = SQLAlchemy()
```

- [ ] **Step 3: Write failing test (`tests/test_web_models.py`)**

```python
import pytest
from flask import Flask

from short_bot.db import init_db, mark_processed, record_short
from short_bot.web.extensions import db
from short_bot.web.models import Short, ProcessedItem, Run, RssItem


@pytest.fixture
def app(tmp_path):
    db_path = tmp_path / "x.sqlite"
    init_db(db_path)
    app = Flask(__name__)
    app.config["SQLALCHEMY_DATABASE_URI"] = f"sqlite:///{db_path}"
    app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False
    db.init_app(app)
    return app


def test_models_map_to_existing_schema(app):
    """ORM models should query Phase 1 tables without `db.create_all()` conflict."""
    with app.app_context():
        # Tables already exist from init_db; just query them
        assert ProcessedItem.query.count() == 0
        assert Short.query.count() == 0
        assert Run.query.count() == 0
        assert RssItem.query.count() == 0


def test_short_file_url_property(app):
    with app.app_context():
        from short_bot.db import init_db as _init
        eng = _init(app.config["SQLALCHEMY_DATABASE_URI"].replace("sqlite:///", ""))
        sid = record_short(
            eng, channel="test", rss_item_guid="g1", title="X",
            file_path="output/test/2026-05-05_x.mp4", duration_s=6,
            script_json="{}", render_ms=1000,
        )
    with app.app_context():
        s = Short.query.get(sid)
        assert s.file_url == "/output/test/2026-05-05_x.mp4"
```

- [ ] **Step 4: Run, expect FAIL**

```bash
python -m pytest tests/test_web_models.py -v
```
Expected: ImportError on `short_bot.web.models`.

- [ ] **Step 5: Implement `src/short_bot/web/models.py`**

```python
"""SQLAlchemy ORM models that map onto Phase 1's SQLite schema.

These tables already exist (created by `short_bot.db.init_db()`). The ORM
classes here are read/write mappings — `db.create_all()` is a no-op since
the schema is identical.
"""
from datetime import datetime
from pathlib import Path

from short_bot.web.extensions import db


class ProcessedItem(db.Model):
    __tablename__ = "processed_items"
    guid = db.Column(db.String, primary_key=True)
    title = db.Column(db.Text, nullable=False)
    channel = db.Column(db.String, nullable=False)
    processed_at = db.Column(db.DateTime, default=datetime.utcnow)


class RssItem(db.Model):
    __tablename__ = "rss_items"
    id = db.Column(db.Integer, primary_key=True)
    guid = db.Column(db.String, nullable=False)
    channel = db.Column(db.String, nullable=False)
    title = db.Column(db.Text, nullable=False)
    link = db.Column(db.Text, nullable=False)
    source = db.Column(db.String)
    pub_date = db.Column(db.DateTime)
    thumb_url = db.Column(db.Text)
    score = db.Column(db.Float)
    status = db.Column(db.String)
    fetched_at = db.Column(db.DateTime, default=datetime.utcnow)
    short_id = db.Column(db.Integer, db.ForeignKey("shorts.id"))


class Short(db.Model):
    __tablename__ = "shorts"
    id = db.Column(db.Integer, primary_key=True)
    channel = db.Column(db.String, nullable=False)
    rss_item_guid = db.Column(db.String)
    title = db.Column(db.Text, nullable=False)
    file_path = db.Column(db.Text, nullable=False)
    duration_s = db.Column(db.Integer)
    script_json = db.Column(db.Text)
    render_ms = db.Column(db.Integer)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    deleted_at = db.Column(db.DateTime)

    @property
    def file_url(self) -> str:
        """Canonical URL the panel uses to serve the mp4."""
        return f"/output/{self.channel}/{Path(self.file_path).name}"


class Run(db.Model):
    __tablename__ = "runs"
    id = db.Column(db.Integer, primary_key=True)
    channel = db.Column(db.String, nullable=False)
    trigger = db.Column(db.String, nullable=False)
    started_at = db.Column(db.DateTime, default=datetime.utcnow)
    ended_at = db.Column(db.DateTime)
    status = db.Column(db.String)
    short_id = db.Column(db.Integer, db.ForeignKey("shorts.id"))
    error = db.Column(db.Text)
    log_path = db.Column(db.Text)
```

- [ ] **Step 6: Run, expect PASS**

```bash
python -m pytest tests/test_web_models.py -v
```
Expected: 2 passed.

- [ ] **Step 7: Commit**

```bash
git add src/short_bot/web/__init__.py src/short_bot/web/extensions.py src/short_bot/web/models.py tests/test_web_models.py
git commit -m "feat(web): SQLAlchemy ORM models mapped onto Phase 1 schema"
```

---

## Task 3: Flask app factory + base template

**Files:**
- Modify: `src/short_bot/web/__init__.py` (replace placeholder with factory)
- Create: `src/short_bot/web/templates/base.html.j2`
- Test: `tests/test_web_app_factory.py`

- [ ] **Step 1: Write failing test (`tests/test_web_app_factory.py`)**

```python
from short_bot.web import create_app


def test_create_app_returns_flask(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    db_path = tmp_path / "x.sqlite"
    app = create_app(config_dir=cfg_dir, db_path=db_path, scheduler=False)
    assert app is not None
    assert app.config["SQLALCHEMY_DATABASE_URI"] == f"sqlite:///{db_path}"


def test_app_has_blueprints(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    app = create_app(config_dir=cfg_dir, db_path=tmp_path / "x.sqlite", scheduler=False)
    expected = {"dashboard", "shorts", "rss", "channels", "channel_new",
                "channel_edit", "preview", "logs"}
    assert expected <= set(app.blueprints.keys())


def test_root_route_returns_200(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    app = create_app(config_dir=cfg_dir, db_path=tmp_path / "x.sqlite", scheduler=False)
    client = app.test_client()
    resp = client.get("/")
    assert resp.status_code == 200
    assert b"short-bot" in resp.data.lower()
```

- [ ] **Step 2: Run, expect FAIL**

```bash
python -m pytest tests/test_web_app_factory.py -v
```
Expected: ImportError on `create_app`.

- [ ] **Step 3: Implement `src/short_bot/web/__init__.py` (factory)**

```python
"""short-bot web panel: Flask app factory + scheduler boot.

Usage:
    from short_bot.web import create_app
    app = create_app(config_dir=Path("config"), db_path=Path("data/short_bot.sqlite"))
    app.run(host="127.0.0.1", port=5005)
"""
from __future__ import annotations

from pathlib import Path

from flask import Flask

from short_bot.config import load_settings
from short_bot.db import init_db
from short_bot.web.extensions import db


def create_app(
    *,
    config_dir: Path | str = "config",
    db_path: Path | str = "data/short_bot.sqlite",
    templates_dir: Path | str = "templates",
    music_root: Path | str = "assets/music",
    cache_dir: Path | str = "data/cache",
    lock_dir: Path | str = "data/locks",
    logs_dir: Path | str = "logs/runs",
    output_root: Path | str = "output",
    scheduler: bool = True,
) -> Flask:
    """Construct the Flask app. `scheduler=False` for tests."""
    config_dir = Path(config_dir)
    db_path = Path(db_path)

    # Ensure schema exists (Phase 1+2 init_db is idempotent + adds pragmas)
    init_db(db_path)

    app = Flask(
        __name__,
        template_folder="templates",
        static_folder="static",
    )
    app.config["SQLALCHEMY_DATABASE_URI"] = f"sqlite:///{db_path}"
    app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False
    app.config["SECRET_KEY"] = "dev-key-change-me"   # dev only; localhost-only

    # Stash paths for blueprints
    app.config["SHORTBOT_CONFIG_DIR"] = config_dir
    app.config["SHORTBOT_DB_PATH"] = db_path
    app.config["SHORTBOT_TEMPLATES_DIR"] = Path(templates_dir)
    app.config["SHORTBOT_MUSIC_ROOT"] = Path(music_root)
    app.config["SHORTBOT_CACHE_DIR"] = Path(cache_dir)
    app.config["SHORTBOT_LOCK_DIR"] = Path(lock_dir)
    app.config["SHORTBOT_LOGS_DIR"] = Path(logs_dir)
    app.config["SHORTBOT_OUTPUT_ROOT"] = Path(output_root)
    app.config["SHORTBOT_SETTINGS"] = load_settings(config_dir / "settings.yaml")

    db.init_app(app)

    # Register blueprints
    from short_bot.web.routes import register_blueprints
    register_blueprints(app)

    if scheduler:
        from short_bot.web.scheduler import init_scheduler
        init_scheduler(app)

    return app
```

- [ ] **Step 4: Create `src/short_bot/web/routes/__init__.py`**

```python
"""Blueprint registration."""
from flask import Flask


def register_blueprints(app: Flask) -> None:
    from short_bot.web.routes import (
        dashboard, shorts, rss, channels,
        channel_new, channel_edit, preview, logs,
    )
    app.register_blueprint(dashboard.bp)
    app.register_blueprint(shorts.bp)
    app.register_blueprint(rss.bp)
    app.register_blueprint(channels.bp)
    app.register_blueprint(channel_new.bp)
    app.register_blueprint(channel_edit.bp)
    app.register_blueprint(preview.bp)
    app.register_blueprint(logs.bp)
```

- [ ] **Step 5: Create `src/short_bot/web/templates/base.html.j2`**

```html
<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}short-bot{% endblock %}</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/htmx.org@2.0.4"></script>
  <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
  <style>
    [x-cloak] { display: none !important; }
  </style>
</head>
<body class="bg-slate-900 text-slate-100 min-h-screen">
  <nav class="bg-slate-950 border-b border-slate-800 px-6 py-3 flex items-center gap-6">
    <a href="/" class="font-bold text-red-500">▶ short-bot</a>
    <a href="/" class="text-slate-300 hover:text-white">Dashboard</a>
    <a href="/shorts" class="text-slate-300 hover:text-white">Shorts</a>
    <a href="/rss" class="text-slate-300 hover:text-white">RSS</a>
    <a href="/channels" class="text-slate-300 hover:text-white">Kanallar</a>
    <a href="/logs" class="text-slate-300 hover:text-white">Loglar</a>
  </nav>
  <main class="container mx-auto px-6 py-6 max-w-7xl">
    {% block content %}{% endblock %}
  </main>
</body>
</html>
```

- [ ] **Step 6: Create stub blueprints**

For each of these, create the file with minimal content:

`src/short_bot/web/routes/dashboard.py`:
```python
from flask import Blueprint, render_template

bp = Blueprint("dashboard", __name__)


@bp.route("/")
def index():
    return render_template("dashboard.html.j2")
```

Create `src/short_bot/web/templates/dashboard.html.j2`:
```html
{% extends "base.html.j2" %}
{% block title %}short-bot — Dashboard{% endblock %}
{% block content %}
<h1 class="text-3xl font-bold mb-4">Dashboard</h1>
<p class="text-slate-400">(content TBD in next tasks)</p>
{% endblock %}
```

For shorts/rss/channels/channel_new/channel_edit/preview/logs, create stub `bp = Blueprint(...)` with one stub route returning a placeholder string. Example for shorts:

`src/short_bot/web/routes/shorts.py`:
```python
from flask import Blueprint

bp = Blueprint("shorts", __name__)


@bp.route("/shorts")
def list_view():
    return "Shorts (stub)"
```

Repeat for `rss.py`, `channels.py`, `channel_new.py`, `channel_edit.py`, `preview.py`, `logs.py` — each with `bp = Blueprint("<name>", __name__)` and a single stub route. The names must match what `register_blueprints` imports:
- `rss.py`: `bp = Blueprint("rss", __name__)`, `@bp.route("/rss")`
- `channels.py`: `bp = Blueprint("channels", __name__)`, `@bp.route("/channels")`
- `channel_new.py`: `bp = Blueprint("channel_new", __name__)`, `@bp.route("/channels/new")`
- `channel_edit.py`: `bp = Blueprint("channel_edit", __name__)`, `@bp.route("/channels/<slug>/edit")`
- `preview.py`: `bp = Blueprint("preview", __name__)`, `@bp.route("/preview/<slug>")`
- `logs.py`: `bp = Blueprint("logs", __name__)`, `@bp.route("/logs")`

Each stub route just returns a placeholder string.

- [ ] **Step 7: Run, expect PASS**

```bash
python -m pytest tests/test_web_app_factory.py -v
```
Expected: 3 passed.

- [ ] **Step 8: Commit**

```bash
git add src/short_bot/web/
git commit -m "feat(web): Flask app factory + base template + stub blueprints"
```

---

## Task 4: CLI `web` subcommand

**Files:**
- Modify: `src/short_bot/cli.py`
- Test: `tests/test_cli.py`

- [ ] **Step 1: Append failing test (`tests/test_cli.py`)**

```python
def test_web_subcommand_help():
    result = subprocess.run(
        [sys.executable, "-m", "short_bot", "web", "--help"],
        capture_output=True, text=True, encoding="utf-8",
    )
    assert result.returncode == 0
    assert "host" in result.stdout.lower() or "port" in result.stdout.lower()
```

- [ ] **Step 2: Run, expect FAIL**

```bash
python -m pytest tests/test_cli.py::test_web_subcommand_help -v
```
Expected: returncode != 0.

- [ ] **Step 3: Add `_add_web` and `_cmd_web` to `src/short_bot/cli.py`**

In `cli.py`, find the line `_add_create_channel(sub)` near the end of `main()` and add `_add_web(sub)` after it.

Then add these functions (e.g. before `def main(...)`):

```python
def _add_web(sub):
    p = sub.add_parser("web", help="Start the web panel + scheduler")
    p.add_argument("--host", default=None, help="Bind host (default from settings)")
    p.add_argument("--port", type=int, default=None, help="Bind port (default from settings)")
    p.add_argument("--config-dir", default="config")
    p.add_argument("--data-dir", default="data")
    p.add_argument("--templates-dir", default="templates")
    p.add_argument("--music-root", default="assets/music")
    p.add_argument("--logs-dir", default="logs/runs")
    p.add_argument("--output-root", default="output")
    p.add_argument("--no-scheduler", action="store_true",
                    help="Disable APScheduler (panel-only)")
    p.set_defaults(func=_cmd_web)


def _cmd_web(args) -> int:
    from pathlib import Path
    from short_bot.web import create_app

    config_dir = Path(args.config_dir)
    data_dir = Path(args.data_dir)
    settings = load_settings(config_dir / "settings.yaml")

    host = args.host or settings.web_host
    port = args.port or settings.web_port

    app = create_app(
        config_dir=config_dir,
        db_path=data_dir / "short_bot.sqlite",
        templates_dir=Path(args.templates_dir),
        music_root=Path(args.music_root),
        cache_dir=data_dir / "cache",
        lock_dir=data_dir / "locks",
        logs_dir=Path(args.logs_dir),
        output_root=Path(args.output_root),
        scheduler=not args.no_scheduler,
    )

    print(f"Web panel: http://{host}:{port}")
    if not args.no_scheduler:
        print(f"Scheduler: see /channels")
    print("Press Ctrl+C to stop.")
    app.run(host=host, port=port, debug=False, use_reloader=False)
    return 0
```

- [ ] **Step 4: Run tests, expect PASS**

```bash
python -m pytest tests/test_cli.py -v
```
Expected: existing CLI tests + new test passing.

- [ ] **Step 5: Commit**

```bash
git add src/short_bot/cli.py tests/test_cli.py
git commit -m "feat(cli): web subcommand starts Flask + scheduler"
```

---

## Task 5: Dashboard route + counters

**Files:**
- Modify: `src/short_bot/web/routes/dashboard.py`
- Modify: `src/short_bot/web/templates/dashboard.html.j2`
- Test: `tests/test_web_dashboard.py`

- [ ] **Step 1: Write failing test (`tests/test_web_dashboard.py`)**

```python
import pytest
from datetime import datetime, timedelta

from short_bot.db import init_db, record_short, start_run, finish_run
from short_bot.web import create_app


@pytest.fixture
def app(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    db_path = tmp_path / "x.sqlite"
    eng = init_db(db_path)
    # Seed: 2 shorts today + 1 yesterday + 1 failed run
    record_short(eng, channel="ch", rss_item_guid="g1", title="A",
                 file_path="output/ch/a.mp4", duration_s=6,
                 script_json="{}", render_ms=1000)
    record_short(eng, channel="ch", rss_item_guid="g2", title="B",
                 file_path="output/ch/b.mp4", duration_s=6,
                 script_json="{}", render_ms=1000)
    rid = start_run(eng, "ch", trigger="cron", log_path="x.log")
    finish_run(eng, rid, status="failed", short_id=None, error="boom")
    return create_app(config_dir=cfg_dir, db_path=db_path, scheduler=False)


def test_dashboard_shows_total_count(app):
    client = app.test_client()
    resp = client.get("/")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "2" in body  # 2 shorts total


def test_dashboard_shows_channel_count(app):
    client = app.test_client()
    resp = client.get("/")
    body = resp.data.decode("utf-8")
    assert "Kanal" in body or "channel" in body.lower()


def test_dashboard_shows_failed_runs_count(app):
    client = app.test_client()
    resp = client.get("/")
    body = resp.data.decode("utf-8")
    assert "1" in body  # 1 failed run
```

- [ ] **Step 2: Run, expect FAIL**

```bash
python -m pytest tests/test_web_dashboard.py -v
```
Expected: FAIL — dashboard is still stub.

- [ ] **Step 3: Implement `src/short_bot/web/routes/dashboard.py`**

```python
from datetime import datetime, timedelta
from pathlib import Path

from flask import Blueprint, current_app, render_template
from sqlalchemy import func

from short_bot.config import list_channels
from short_bot.web.extensions import db
from short_bot.web.models import Run, Short

bp = Blueprint("dashboard", __name__)


@bp.route("/")
def index():
    today = datetime.utcnow().date()
    today_start = datetime.combine(today, datetime.min.time())

    total_shorts = Short.query.filter(Short.deleted_at.is_(None)).count()
    today_shorts = Short.query.filter(
        Short.deleted_at.is_(None),
        Short.created_at >= today_start,
    ).count()
    failed_runs_24h = Run.query.filter(
        Run.status == "failed",
        Run.started_at >= datetime.utcnow() - timedelta(hours=24),
    ).count()

    channels = list_channels(current_app.config["SHORTBOT_CONFIG_DIR"] / "channels",
                              enabled_only=False)
    recent = (Short.query
              .filter(Short.deleted_at.is_(None))
              .order_by(Short.created_at.desc())
              .limit(4).all())

    return render_template(
        "dashboard.html.j2",
        total_shorts=total_shorts,
        today_shorts=today_shorts,
        failed_runs=failed_runs_24h,
        channel_count=len(channels),
        channels=channels,
        recent=recent,
    )
```

- [ ] **Step 4: Implement `src/short_bot/web/templates/dashboard.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — Dashboard{% endblock %}
{% block content %}
<h1 class="text-3xl font-bold mb-6">Dashboard</h1>

<div class="grid grid-cols-4 gap-4 mb-8">
  <div class="bg-slate-800 rounded-lg p-4">
    <div class="text-xs text-slate-400 uppercase tracking-wider">Bugün üretilen</div>
    <div class="text-3xl font-bold mt-1">{{ today_shorts }}</div>
  </div>
  <div class="bg-slate-800 rounded-lg p-4">
    <div class="text-xs text-slate-400 uppercase tracking-wider">Toplam Shorts</div>
    <div class="text-3xl font-bold mt-1">{{ total_shorts }}</div>
  </div>
  <div class="bg-slate-800 rounded-lg p-4">
    <div class="text-xs text-slate-400 uppercase tracking-wider">Kanal</div>
    <div class="text-3xl font-bold mt-1">{{ channel_count }}</div>
  </div>
  <div class="bg-slate-800 rounded-lg p-4">
    <div class="text-xs text-slate-400 uppercase tracking-wider">Hata (24s)</div>
    <div class="text-3xl font-bold mt-1 {{ 'text-red-500' if failed_runs > 0 else 'text-green-500' }}">
      {{ failed_runs }}
    </div>
  </div>
</div>

<h2 class="text-xl font-bold mb-3">Son üretilenler</h2>
<div class="grid grid-cols-4 gap-4">
  {% for s in recent %}
  <div class="bg-slate-800 rounded-lg overflow-hidden">
    <div class="aspect-[9/16] bg-slate-900 flex items-center justify-center text-slate-500 text-xs">
      <a href="/shorts/{{ s.id }}" class="block w-full h-full flex items-center justify-center">▶ {{ s.title }}</a>
    </div>
    <div class="p-3 text-xs">
      <div class="font-semibold truncate">{{ s.title }}</div>
      <div class="text-slate-400 mt-1">{{ s.channel }} · {{ s.created_at.strftime('%H:%M') }}</div>
    </div>
  </div>
  {% else %}
  <div class="col-span-4 text-slate-500 text-center py-8">Henüz Short yok.</div>
  {% endfor %}
</div>
{% endblock %}
```

- [ ] **Step 5: Run, expect PASS**

```bash
python -m pytest tests/test_web_dashboard.py -v
```
Expected: 3 passed.

- [ ] **Step 6: Commit**

```bash
git add src/short_bot/web/routes/dashboard.py src/short_bot/web/templates/dashboard.html.j2 tests/test_web_dashboard.py
git commit -m "feat(web): Dashboard with counters + recent shorts grid"
```

---

## Task 6: Shorts gallery

**Files:**
- Modify: `src/short_bot/web/routes/shorts.py`
- Create: `src/short_bot/web/templates/shorts/list.html.j2`
- Create: `src/short_bot/web/templates/_partials/shorts_grid.html.j2`
- Test: `tests/test_web_shorts.py`

- [ ] **Step 1: Write failing test (`tests/test_web_shorts.py`)**

```python
import pytest

from short_bot.db import init_db, record_short
from short_bot.web import create_app


@pytest.fixture
def app(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    db_path = tmp_path / "x.sqlite"
    eng = init_db(db_path)
    record_short(eng, channel="ch1", rss_item_guid="g1", title="Alpha",
                 file_path="output/ch1/a.mp4", duration_s=6, script_json="{}", render_ms=1000)
    record_short(eng, channel="ch2", rss_item_guid="g2", title="Beta",
                 file_path="output/ch2/b.mp4", duration_s=6, script_json="{}", render_ms=1000)
    return create_app(config_dir=cfg_dir, db_path=db_path, scheduler=False)


def test_shorts_list_shows_all(app):
    client = app.test_client()
    resp = client.get("/shorts")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "Alpha" in body
    assert "Beta" in body


def test_shorts_filter_by_channel(app):
    client = app.test_client()
    resp = client.get("/shorts/grid?channel=ch1")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "Alpha" in body
    assert "Beta" not in body


def test_shorts_search_query(app):
    client = app.test_client()
    resp = client.get("/shorts/grid?q=Alpha")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "Alpha" in body
    assert "Beta" not in body
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/routes/shorts.py`**

```python
from flask import Blueprint, render_template, request

from short_bot.web.models import Short

bp = Blueprint("shorts", __name__)


@bp.route("/shorts")
def list_view():
    return render_template("shorts/list.html.j2",
                            channel=request.args.get("channel", ""),
                            q=request.args.get("q", ""))


@bp.route("/shorts/grid")
def grid_partial():
    """HTMX partial: filter result grid swap."""
    channel = request.args.get("channel", "").strip()
    q = request.args.get("q", "").strip()
    query = Short.query.filter(Short.deleted_at.is_(None))
    if channel:
        query = query.filter(Short.channel == channel)
    if q:
        query = query.filter(Short.title.ilike(f"%{q}%"))
    shorts = query.order_by(Short.created_at.desc()).limit(60).all()
    return render_template("_partials/shorts_grid.html.j2", shorts=shorts)
```

- [ ] **Step 4: Implement `src/short_bot/web/templates/shorts/list.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — Shorts{% endblock %}
{% block content %}
<div class="flex justify-between items-center mb-4">
  <h1 class="text-3xl font-bold">Shorts</h1>
</div>

<div class="flex gap-3 mb-4 items-center">
  <input type="text" name="q" value="{{ q }}" placeholder="Başlık ara..."
         class="bg-slate-800 px-3 py-2 rounded text-sm flex-1"
         hx-get="/shorts/grid" hx-target="#grid" hx-trigger="keyup changed delay:300ms"
         hx-include="[name='channel']">
  <select name="channel" class="bg-slate-800 px-3 py-2 rounded text-sm"
          hx-get="/shorts/grid" hx-target="#grid" hx-trigger="change"
          hx-include="[name='q']">
    <option value="">Tüm kanallar</option>
    <option value="son-dakika" {% if channel == 'son-dakika' %}selected{% endif %}>son-dakika</option>
    <option value="spor-short-de" {% if channel == 'spor-short-de' %}selected{% endif %}>spor-short-de</option>
  </select>
</div>

<div id="grid" hx-get="/shorts/grid?channel={{ channel }}&q={{ q }}" hx-trigger="load">
  Yükleniyor...
</div>
{% endblock %}
```

- [ ] **Step 5: Implement `src/short_bot/web/templates/_partials/shorts_grid.html.j2`**

```html
<div class="grid grid-cols-4 gap-4">
  {% for s in shorts %}
  <div class="bg-slate-800 rounded-lg overflow-hidden">
    <a href="/shorts/{{ s.id }}" class="block">
      <div class="aspect-[9/16] bg-slate-900 flex items-center justify-center text-slate-500 text-xs">
        ▶ {{ s.title[:30] }}
      </div>
    </a>
    <div class="p-3 text-xs">
      <div class="font-semibold truncate">{{ s.title }}</div>
      <div class="text-slate-400 mt-1">{{ s.channel }} · {{ s.created_at.strftime('%d %b %H:%M') }}</div>
    </div>
  </div>
  {% else %}
  <div class="col-span-4 text-slate-500 text-center py-12">Sonuç yok.</div>
  {% endfor %}
</div>
```

- [ ] **Step 6: Run tests, expect PASS**

```bash
python -m pytest tests/test_web_shorts.py -v
```
Expected: 3 passed.

- [ ] **Step 7: Commit**

```bash
git add src/short_bot/web/routes/shorts.py src/short_bot/web/templates/shorts/ src/short_bot/web/templates/_partials/shorts_grid.html.j2 tests/test_web_shorts.py
git commit -m "feat(web): Shorts gallery with HTMX filter (channel + search)"
```

---

## Task 7: Short detail + serve mp4

**Files:**
- Modify: `src/short_bot/web/routes/shorts.py`
- Create: `src/short_bot/web/templates/shorts/detail.html.j2`
- Modify: `src/short_bot/web/__init__.py` (mp4 static-file route)
- Test: `tests/test_web_shorts.py` (append)

- [ ] **Step 1: Append failing tests**

```python
def test_short_detail_view(app):
    client = app.test_client()
    resp = client.get("/shorts/1")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "Alpha" in body
    assert "ch1" in body


def test_short_detail_404_when_missing(app):
    client = app.test_client()
    resp = client.get("/shorts/9999")
    assert resp.status_code == 404
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Add detail route to `shorts.py`**

Append to `src/short_bot/web/routes/shorts.py`:

```python
from flask import abort


@bp.route("/shorts/<int:short_id>")
def detail(short_id):
    s = Short.query.filter_by(id=short_id).first()
    if s is None or s.deleted_at is not None:
        abort(404)
    return render_template("shorts/detail.html.j2", s=s)
```

- [ ] **Step 4: Create `src/short_bot/web/templates/shorts/detail.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — {{ s.title }}{% endblock %}
{% block content %}
<a href="/shorts" class="text-slate-400 hover:text-white text-sm">← Shorts</a>
<h1 class="text-3xl font-bold mt-2 mb-1">{{ s.title }}</h1>
<div class="text-slate-400 text-sm mb-4">
  {{ s.channel }} · {{ s.created_at.strftime('%d %b %Y, %H:%M') }} · {{ s.duration_s }}s · render {{ s.render_ms }}ms
</div>

<div class="grid grid-cols-3 gap-6">
  <div class="col-span-1">
    <video controls class="w-full bg-black rounded">
      <source src="{{ s.file_url }}" type="video/mp4">
    </video>
  </div>
  <div class="col-span-2 space-y-3">
    <div class="bg-slate-800 rounded p-4">
      <div class="text-xs text-slate-400 uppercase tracking-wider mb-1">Dosya</div>
      <div class="font-mono text-xs">{{ s.file_path }}</div>
    </div>
    <div class="bg-slate-800 rounded p-4">
      <div class="text-xs text-slate-400 uppercase tracking-wider mb-1">RSS GUID</div>
      <div class="font-mono text-xs break-all">{{ s.rss_item_guid }}</div>
    </div>
    <div class="bg-slate-800 rounded p-4">
      <div class="text-xs text-slate-400 uppercase tracking-wider mb-1">Script JSON</div>
      <pre class="text-xs whitespace-pre-wrap">{{ s.script_json }}</pre>
    </div>
  </div>
</div>
{% endblock %}
```

- [ ] **Step 5: Add mp4 static-file route in `src/short_bot/web/__init__.py`**

In `create_app(...)`, after blueprints are registered, add:

```python
    @app.route("/output/<path:filename>")
    def serve_output(filename):
        from flask import send_from_directory
        return send_from_directory(app.config["SHORTBOT_OUTPUT_ROOT"], filename)
```

- [ ] **Step 6: Run, expect PASS**

```bash
python -m pytest tests/test_web_shorts.py -v
```
Expected: 5 passed (3 existing + 2 new).

- [ ] **Step 7: Commit**

```bash
git add src/short_bot/web/routes/shorts.py src/short_bot/web/templates/shorts/detail.html.j2 src/short_bot/web/__init__.py tests/test_web_shorts.py
git commit -m "feat(web): Short detail page + /output/ mp4 static route"
```

---

## Task 8: RSS table

**Files:**
- Modify: `src/short_bot/web/routes/rss.py`
- Create: `src/short_bot/web/templates/rss.html.j2`
- Test: `tests/test_web_rss.py`

- [ ] **Step 1: Write failing test (`tests/test_web_rss.py`)**

```python
import pytest
from datetime import datetime

from short_bot.db import init_db, record_rss_item
from short_bot.web import create_app


@pytest.fixture
def app(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    db_path = tmp_path / "x.sqlite"
    eng = init_db(db_path)
    record_rss_item(eng, guid="g1", channel="ch", title="Big news", link="http://x",
                    source="Reuters", pub_date=datetime.utcnow(), thumb_url=None,
                    score=8.5, status="selected")
    record_rss_item(eng, guid="g2", channel="ch", title="Smaller news", link="http://y",
                    source="AA", pub_date=datetime.utcnow(), thumb_url=None,
                    score=5.0, status="below_threshold")
    return create_app(config_dir=cfg_dir, db_path=db_path, scheduler=False)


def test_rss_page_lists_items(app):
    client = app.test_client()
    resp = client.get("/rss")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "Big news" in body
    assert "Smaller news" in body
    assert "8.5" in body
    assert "selected" in body or "SEÇİLDİ" in body
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/routes/rss.py`**

```python
from datetime import datetime, timedelta

from flask import Blueprint, render_template, request

from short_bot.web.models import RssItem

bp = Blueprint("rss", __name__)


@bp.route("/rss")
def list_view():
    cutoff = datetime.utcnow() - timedelta(hours=24)
    items = (RssItem.query
              .filter(RssItem.fetched_at >= cutoff)
              .order_by(RssItem.score.desc().nullslast(), RssItem.fetched_at.desc())
              .limit(200).all())
    return render_template("rss.html.j2", items=items)
```

- [ ] **Step 4: Implement `src/short_bot/web/templates/rss.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — RSS{% endblock %}
{% block content %}
<h1 class="text-3xl font-bold mb-4">RSS taraması (son 24s)</h1>

<table class="w-full text-xs">
  <thead class="bg-slate-800">
    <tr class="text-left">
      <th class="p-2">Skor</th>
      <th class="p-2">Başlık</th>
      <th class="p-2">Kaynak</th>
      <th class="p-2">Kanal</th>
      <th class="p-2">Saat</th>
      <th class="p-2">Durum</th>
    </tr>
  </thead>
  <tbody>
    {% for it in items %}
    <tr class="border-b border-slate-800 hover:bg-slate-800/50">
      <td class="p-2 font-bold {% if it.score and it.score >= 8 %}text-green-400{% elif it.score and it.score >= 6 %}text-yellow-400{% else %}text-slate-500{% endif %}">
        {{ '%.1f'|format(it.score) if it.score else '—' }}
      </td>
      <td class="p-2">{{ it.title }}</td>
      <td class="p-2 text-slate-400">{{ it.source or '—' }}</td>
      <td class="p-2 text-slate-400">{{ it.channel }}</td>
      <td class="p-2 text-slate-400">{{ it.fetched_at.strftime('%H:%M') }}</td>
      <td class="p-2">
        {% if it.status == 'selected' %}
          <span class="bg-green-700 text-white px-2 py-0.5 rounded text-[10px] uppercase">selected</span>
        {% elif it.status == 'below_threshold' %}
          <span class="text-slate-500">below threshold</span>
        {% elif it.status == 'duplicate' %}
          <span class="text-red-400">duplicate</span>
        {% else %}
          <span class="text-slate-500">{{ it.status or '—' }}</span>
        {% endif %}
      </td>
    </tr>
    {% else %}
    <tr><td colspan="6" class="p-8 text-center text-slate-500">Son 24 saatte RSS item yok.</td></tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}
```

- [ ] **Step 5: Run tests, expect PASS**

```bash
python -m pytest tests/test_web_rss.py -v
```

- [ ] **Step 6: Commit**

```bash
git add src/short_bot/web/routes/rss.py src/short_bot/web/templates/rss.html.j2 tests/test_web_rss.py
git commit -m "feat(web): RSS table — last 24h scoring history"
```

---

## Task 9: Channels list

**Files:**
- Modify: `src/short_bot/web/routes/channels.py`
- Create: `src/short_bot/web/templates/channels/list.html.j2`
- Test: `tests/test_web_channels.py`

- [ ] **Step 1: Write failing test (`tests/test_web_channels.py`)**

```python
import pytest

from short_bot.web import create_app


@pytest.fixture
def app(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    (cfg_dir / "channels" / "demo-tr.yaml").write_text(
        "slug: demo-tr\nname: Demo TR\nlanguage: tr\nkeywords: [a]\n"
        "schedule_cron: '0 * * * *'\nduration_s: 6\nmin_score: 6.0\n"
        "max_candidates_per_run: 10\ntemplate: newscast\n"
        "colors: {primary: '#c81e1e', accent: '#ffea3b', bg_gradient: ['#1a3b6b','#0a1a3b']}\n"
        "handle: '@demo'\noutput_dir: output/demo\nenabled: true\n"
        "cta: {enabled: false, text: '', icons: [], duration_s: 0, show_handle: false}\n",
        encoding="utf-8",
    )
    return create_app(config_dir=cfg_dir, db_path=tmp_path / "x.sqlite", scheduler=False)


def test_channels_list_shows_channel(app):
    client = app.test_client()
    resp = client.get("/channels")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "demo-tr" in body
    assert "Demo TR" in body
    assert "newscast" in body
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/routes/channels.py`**

```python
from flask import Blueprint, current_app, render_template

from short_bot.config import list_channels

bp = Blueprint("channels", __name__)


@bp.route("/channels")
def list_view():
    config_dir = current_app.config["SHORTBOT_CONFIG_DIR"]
    channels = list_channels(config_dir / "channels", enabled_only=False)
    return render_template("channels/list.html.j2", channels=channels)
```

- [ ] **Step 4: Implement `src/short_bot/web/templates/channels/list.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — Kanallar{% endblock %}
{% block content %}
<div class="flex justify-between items-center mb-4">
  <h1 class="text-3xl font-bold">Kanallar</h1>
  <a href="/channels/new" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded font-semibold text-sm">
    + Yeni Kanal
  </a>
</div>

<table class="w-full text-sm">
  <thead class="bg-slate-800">
    <tr class="text-left">
      <th class="p-3">Slug</th>
      <th class="p-3">İsim</th>
      <th class="p-3">Dil</th>
      <th class="p-3">Archetype</th>
      <th class="p-3">Cron</th>
      <th class="p-3">DNA</th>
      <th class="p-3">Aktif</th>
      <th class="p-3"></th>
    </tr>
  </thead>
  <tbody>
    {% for c in channels %}
    <tr class="border-b border-slate-800 hover:bg-slate-800/50">
      <td class="p-3 font-mono text-xs">{{ c.slug }}</td>
      <td class="p-3">{{ c.name }}</td>
      <td class="p-3">{{ c.language }}</td>
      <td class="p-3 text-slate-400">{{ c.template }}</td>
      <td class="p-3 font-mono text-xs">{{ c.schedule_cron }}</td>
      <td class="p-3">
        {% if c.dna %}
          <span class="bg-green-700 text-white px-2 py-0.5 rounded text-xs">DNA</span>
        {% else %}
          <span class="text-slate-500">—</span>
        {% endif %}
      </td>
      <td class="p-3">{{ '✓' if c.enabled else '✗' }}</td>
      <td class="p-3">
        <a href="/channels/{{ c.slug }}/edit" class="text-blue-400 hover:underline">Düzenle</a>
      </td>
    </tr>
    {% else %}
    <tr><td colspan="8" class="p-8 text-center text-slate-500">Henüz kanal yok.</td></tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}
```

- [ ] **Step 5: Run, expect PASS**

```bash
python -m pytest tests/test_web_channels.py -v
```

- [ ] **Step 6: Commit**

```bash
git add src/short_bot/web/routes/channels.py src/short_bot/web/templates/channels/list.html.j2 tests/test_web_channels.py
git commit -m "feat(web): Channels list with archetype/lang/DNA badges"
```

---

## Task 10: Logs page + tail polling

**Files:**
- Modify: `src/short_bot/web/routes/logs.py`
- Create: `src/short_bot/web/templates/logs.html.j2`
- Create: `src/short_bot/web/templates/_partials/log_tail.html.j2`
- Test: `tests/test_web_logs.py`

- [ ] **Step 1: Write failing test (`tests/test_web_logs.py`)**

```python
import pytest

from short_bot.web import create_app


@pytest.fixture
def app(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    logs_dir = tmp_path / "logs" / "runs"
    logs_dir.mkdir(parents=True)
    (logs_dir / "20260505_140000_son-dakika.log").write_text(
        "line A\nline B\nline C\n", encoding="utf-8"
    )
    return create_app(config_dir=cfg_dir, db_path=tmp_path / "x.sqlite",
                      logs_dir=logs_dir, scheduler=False)


def test_logs_page_renders(app):
    client = app.test_client()
    resp = client.get("/logs")
    assert resp.status_code == 200


def test_logs_tail_returns_recent_lines(app):
    client = app.test_client()
    resp = client.get("/logs/tail")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "line A" in body or "line B" in body or "line C" in body
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/routes/logs.py`**

```python
from pathlib import Path

from flask import Blueprint, current_app, render_template

bp = Blueprint("logs", __name__)


def _read_recent_lines(logs_dir: Path, max_lines: int = 200) -> list[str]:
    files = sorted(logs_dir.glob("*.log"), key=lambda p: p.stat().st_mtime, reverse=True)
    out: list[str] = []
    for fp in files[:5]:  # last 5 log files
        try:
            content = fp.read_text(encoding="utf-8", errors="replace").splitlines()
            out.extend([f"[{fp.stem}] {line}" for line in content[-50:]])
            if len(out) >= max_lines:
                break
        except OSError:
            continue
    return out[-max_lines:]


@bp.route("/logs")
def list_view():
    return render_template("logs.html.j2")


@bp.route("/logs/tail")
def tail_partial():
    logs_dir = current_app.config["SHORTBOT_LOGS_DIR"]
    lines = _read_recent_lines(logs_dir)
    return render_template("_partials/log_tail.html.j2", lines=lines)
```

- [ ] **Step 4: Implement `src/short_bot/web/templates/logs.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — Loglar{% endblock %}
{% block content %}
<h1 class="text-3xl font-bold mb-4">Loglar</h1>

<div id="logtail" class="bg-slate-950 rounded-lg p-4 font-mono text-xs h-[600px] overflow-y-auto"
     hx-get="/logs/tail" hx-trigger="load, every 2s" hx-swap="innerHTML">
  Yükleniyor...
</div>
{% endblock %}
```

- [ ] **Step 5: Implement `src/short_bot/web/templates/_partials/log_tail.html.j2`**

```html
{% for line in lines %}
<div class="text-slate-300 leading-tight">{{ line }}</div>
{% else %}
<div class="text-slate-500">No logs yet.</div>
{% endfor %}
```

- [ ] **Step 6: Run tests, expect PASS**

```bash
python -m pytest tests/test_web_logs.py -v
```

- [ ] **Step 7: Commit**

```bash
git add src/short_bot/web/routes/logs.py src/short_bot/web/templates/logs.html.j2 src/short_bot/web/templates/_partials/log_tail.html.j2 tests/test_web_logs.py
git commit -m "feat(web): Logs page with HTMX 2s polling tail"
```

---

## Task 11: Channel edit (form-only) + save

**Files:**
- Modify: `src/short_bot/web/routes/channel_edit.py`
- Create: `src/short_bot/web/templates/channels/edit.html.j2`
- Test: `tests/test_web_channels.py` (append)

- [ ] **Step 1: Append failing test**

```python
def test_channel_edit_get_renders(app):
    client = app.test_client()
    resp = client.get("/channels/demo-tr/edit")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "demo-tr" in body
    assert "Demo TR" in body


def test_channel_edit_404_when_missing(app):
    client = app.test_client()
    resp = client.get("/channels/nonexistent/edit")
    assert resp.status_code == 404


def test_channel_edit_post_saves(app, tmp_path):
    client = app.test_client()
    resp = client.post("/channels/demo-tr/edit", data={
        "keywords": "yeni, kelimeler",
        "schedule_cron": "0 8,16 * * *",
        "handle": "@updated",
        "duration_s": "10",
        "min_score": "7.0",
        "enabled": "1",
    })
    assert resp.status_code in (200, 302)
    # Reload and verify persistence
    from short_bot.config import load_channel
    cfg_dir = app.config["SHORTBOT_CONFIG_DIR"]
    cfg = load_channel(cfg_dir / "channels" / "demo-tr.yaml")
    assert cfg.handle == "@updated"
    assert cfg.duration_s == 10
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/routes/channel_edit.py`**

```python
from flask import Blueprint, abort, current_app, redirect, render_template, request, url_for

from short_bot.config import (ChannelConfig, load_channel, save_channel)

bp = Blueprint("channel_edit", __name__)


def _yaml_path(slug: str):
    return current_app.config["SHORTBOT_CONFIG_DIR"] / "channels" / f"{slug}.yaml"


@bp.route("/channels/<slug>/edit", methods=["GET"])
def edit(slug):
    path = _yaml_path(slug)
    if not path.exists():
        abort(404)
    cfg = load_channel(path)
    return render_template("channels/edit.html.j2", c=cfg)


@bp.route("/channels/<slug>/edit", methods=["POST"])
def save(slug):
    path = _yaml_path(slug)
    if not path.exists():
        abort(404)
    cfg = load_channel(path)

    keywords = [k.strip() for k in request.form.get("keywords", "").split(",") if k.strip()]
    new_cfg = ChannelConfig(
        slug=cfg.slug, name=cfg.name, keywords=keywords,
        rss_locale=cfg.rss_locale,
        schedule_cron=request.form.get("schedule_cron", cfg.schedule_cron),
        duration_s=int(request.form.get("duration_s", cfg.duration_s)),
        min_score=float(request.form.get("min_score", cfg.min_score)),
        max_candidates_per_run=cfg.max_candidates_per_run,
        template=cfg.template,
        colors=cfg.colors,
        handle=request.form.get("handle", cfg.handle),
        output_dir=cfg.output_dir,
        enabled=request.form.get("enabled") == "1",
        cta_enabled=cfg.cta_enabled, cta_text=cfg.cta_text,
        cta_icons=cfg.cta_icons, cta_duration_s=cfg.cta_duration_s,
        cta_show_handle=cfg.cta_show_handle,
        language=cfg.language, dna=cfg.dna, script_model=cfg.script_model,
    )
    save_channel(path, new_cfg)
    return redirect(url_for("channel_edit.edit", slug=slug))
```

- [ ] **Step 4: Implement `src/short_bot/web/templates/channels/edit.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — {{ c.slug }}{% endblock %}
{% block content %}
<a href="/channels" class="text-slate-400 hover:text-white text-sm">← Kanallar</a>
<h1 class="text-3xl font-bold mt-2 mb-1">{{ c.name }}</h1>
<div class="text-slate-400 text-sm mb-6">
  {{ c.slug }} · {{ c.language }} · {{ c.template }}
  {% if c.dna %}<span class="bg-green-700 text-white px-2 py-0.5 rounded text-xs ml-2">DNA</span>{% endif %}
</div>

<form method="POST" class="grid grid-cols-2 gap-6">
  <div class="space-y-3">
    <h2 class="text-lg font-bold">Temel</h2>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase tracking-wider">Keywords (virgülle)</span>
      <input type="text" name="keywords" value="{{ c.keywords | join(', ') }}"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase tracking-wider">Cron</span>
      <input type="text" name="schedule_cron" value="{{ c.schedule_cron }}"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 font-mono text-sm">
    </label>

    <div class="grid grid-cols-2 gap-3">
      <label class="block">
        <span class="text-xs text-slate-400 uppercase tracking-wider">Süre (sn)</span>
        <input type="number" name="duration_s" value="{{ c.duration_s }}"
               class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
      </label>
      <label class="block">
        <span class="text-xs text-slate-400 uppercase tracking-wider">Min skor</span>
        <input type="number" step="0.1" name="min_score" value="{{ c.min_score }}"
               class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
      </label>
    </div>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase tracking-wider">Handle</span>
      <input type="text" name="handle" value="{{ c.handle }}"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>

    <label class="flex items-center gap-2">
      <input type="checkbox" name="enabled" value="1" {% if c.enabled %}checked{% endif %}>
      <span>Aktif</span>
    </label>

    <button type="submit" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded font-semibold mt-4">
      💾 Kaydet
    </button>
  </div>

  <div>
    <h2 class="text-lg font-bold mb-3">Önizleme</h2>
    <iframe src="/preview/{{ c.slug }}"
            class="w-full bg-black rounded"
            style="aspect-ratio: 9/16; max-height: 700px;"></iframe>
  </div>
</form>
{% endblock %}
```

- [ ] **Step 5: Run tests, expect PASS**

```bash
python -m pytest tests/test_web_channels.py -v
```

- [ ] **Step 6: Commit**

```bash
git add src/short_bot/web/routes/channel_edit.py src/short_bot/web/templates/channels/edit.html.j2 tests/test_web_channels.py
git commit -m "feat(web): Channel edit form (basic fields) + save"
```

---

## Task 12: Sample script fixtures (5 languages)

**Files:**
- Create: `tests/fixtures/sample_script_<lang>.json` × 5

- [ ] **Step 1: Create `tests/fixtures/sample_script_tr.json`**

```json
{
  "header_top": "ARA ZAM",
  "header_bottom": "GELDİ Mİ?",
  "photo_overlay": "MİLYONLARCA ÇALIŞAN BEKLİYOR",
  "body_paragraph": "Asgari ücrete temmuzda ara zam gelip gelmeyeceği milyonlarca çalışanı yakından ilgilendiriyor. Yüksek enflasyon nedeniyle alım gücü eridi.",
  "highlights": [{"text": "ara zam", "color": "yellow"}],
  "category": "EKONOMİ",
  "mood": "neutral"
}
```

- [ ] **Step 2: Create `tests/fixtures/sample_script_en.json`**

```json
{
  "header_top": "BREAKING",
  "header_bottom": "NEWS!",
  "photo_overlay": "MILLIONS WAITING",
  "body_paragraph": "The interest rate decision is expected this week. Markets are watching closely as inflation pressure builds.",
  "highlights": [{"text": "interest rate", "color": "yellow"}],
  "category": "ECONOMY",
  "mood": "breaking"
}
```

- [ ] **Step 3: Create `tests/fixtures/sample_script_de.json`**

```json
{
  "header_top": "BAYERN",
  "header_bottom": "DREHT 2-1",
  "photo_overlay": "TORSCHUSS NACHSPIELZEIT",
  "body_paragraph": "Bayern verhindert den GAU in Paris mit einem dramatischen Comeback. Der späte Treffer in der Nachspielzeit sichert den Sieg.",
  "highlights": [{"text": "dramatischen Comeback", "color": "yellow"}],
  "category": "FUSSBALL",
  "mood": "breaking"
}
```

- [ ] **Step 4: Create `tests/fixtures/sample_script_es.json`**

```json
{
  "header_top": "ÚLTIMA HORA",
  "header_bottom": "ECONÓMICA",
  "photo_overlay": "MILLONES ESPERANDO",
  "body_paragraph": "La decisión sobre el salario mínimo afectará a millones de trabajadores. La inflación sigue siendo una preocupación importante.",
  "highlights": [{"text": "salario mínimo", "color": "yellow"}],
  "category": "ECONOMÍA",
  "mood": "neutral"
}
```

- [ ] **Step 5: Create `tests/fixtures/sample_script_fr.json`**

```json
{
  "header_top": "DERNIÈRE",
  "header_bottom": "MINUTE!",
  "photo_overlay": "DÉCISION ATTENDUE",
  "body_paragraph": "La décision sur le salaire minimum approche. Des millions de travailleurs attendent une augmentation face à l'inflation.",
  "highlights": [{"text": "salaire minimum", "color": "yellow"}],
  "category": "ÉCONOMIE",
  "mood": "neutral"
}
```

- [ ] **Step 6: Commit**

```bash
git add tests/fixtures/sample_script_*.json
git commit -m "test(fixtures): sample scripts for 5 languages (preview seed data)"
```

---

## Task 13: Preview endpoint

**Files:**
- Modify: `src/short_bot/web/routes/preview.py`
- Test: `tests/test_web_preview.py`

- [ ] **Step 1: Write failing test (`tests/test_web_preview.py`)**

```python
import pytest

from short_bot.web import create_app


@pytest.fixture
def app(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    (cfg_dir / "channels" / "demo-tr.yaml").write_text(
        "slug: demo-tr\nname: Demo\nlanguage: tr\nkeywords: [a]\n"
        "schedule_cron: ''\nduration_s: 6\nmin_score: 6.0\n"
        "max_candidates_per_run: 10\ntemplate: newscast\n"
        "colors: {primary: '#c81e1e', accent: '#ffea3b', bg_gradient: ['#1a3b6b','#0a1a3b']}\n"
        "handle: '@demo'\noutput_dir: output/demo\nenabled: true\n"
        "cta: {enabled: false, text: '', icons: [], duration_s: 0, show_handle: false}\n",
        encoding="utf-8",
    )
    return create_app(config_dir=cfg_dir, db_path=tmp_path / "x.sqlite", scheduler=False)


def test_preview_returns_html(app):
    client = app.test_client()
    resp = client.get("/preview/demo-tr")
    assert resp.status_code == 200
    assert "text/html" in resp.content_type
    body = resp.data.decode("utf-8")
    assert "ARA ZAM" in body  # from sample_script_tr.json
    assert "BEĞEN" in body    # UI labels for tr


def test_preview_404_for_missing_channel(app):
    client = app.test_client()
    resp = client.get("/preview/nonexistent")
    assert resp.status_code == 404


def test_preview_overrides_primary_color(app):
    client = app.test_client()
    resp = client.get("/preview/demo-tr?primary=%23ff00ff")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "#ff00ff" in body
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/routes/preview.py`**

```python
import json
from pathlib import Path

from flask import Blueprint, Response, abort, current_app, request

from short_bot.config import load_channel
from short_bot.locale import ui_labels_for
from short_bot.models import Highlight, RenderJob, Script
from short_bot.renderer import build_html
from short_bot.dna import build_css_override, DnaSpec, DnaPalette, DnaFonts, DnaTone

bp = Blueprint("preview", __name__)


def _load_sample_script(language: str) -> Script:
    fixtures = Path(__file__).resolve().parents[3] / "tests" / "fixtures"
    sample_path = fixtures / f"sample_script_{language}.json"
    if not sample_path.exists():
        sample_path = fixtures / "sample_script_tr.json"
    data = json.loads(sample_path.read_text(encoding="utf-8"))
    data["highlights"] = [Highlight(**h) for h in data.get("highlights", [])]
    return Script(**data)


def _default_dna_for(template: str, channel) -> DnaSpec:
    """Stub DnaSpec when channel has no DNA — uses channel.colors."""
    return DnaSpec(
        archetype=template if template in [
            "newscast", "tabloid", "magazine", "kinetic", "dark-tech", "stadium", "meme"
        ] else "newscast",
        palette=DnaPalette(
            primary=channel.colors["primary"],
            accent=channel.colors["accent"],
            bg_gradient=channel.colors["bg_gradient"],
            body_bg=["#1a1a2a", "#0a0a1a"],
        ),
        fonts=DnaFonts(),
        tone=DnaTone(voice="neutral", style="concise"),
        persona_summary="default",
    )


@bp.route("/preview/<slug>")
def preview(slug):
    cfg_path = current_app.config["SHORTBOT_CONFIG_DIR"] / "channels" / f"{slug}.yaml"
    if not cfg_path.exists():
        abort(404)
    cfg = load_channel(cfg_path)

    dna = cfg.dna or _default_dna_for(cfg.template, cfg)

    # Apply query param overrides (color picker live tweaks)
    if request.args.get("primary"):
        dna = dna.model_copy(update={"palette": dna.palette.model_copy(
            update={"primary": request.args["primary"]})})
    if request.args.get("accent"):
        dna = dna.model_copy(update={"palette": dna.palette.model_copy(
            update={"accent": request.args["accent"]})})
    if request.args.get("font_headline"):
        dna = dna.model_copy(update={"fonts": dna.fonts.model_copy(
            update={"headline": request.args["font_headline"]})})

    script = _load_sample_script(cfg.language)
    job = RenderJob(
        script=script, bg_image_path=None,
        music_path=Path("dummy.mp3"),
        channel_colors={
            "primary": dna.palette.primary,
            "accent": dna.palette.accent,
            "bg_gradient": dna.palette.bg_gradient,
        },
        handle=cfg.handle, duration_s=cfg.duration_s,
        language=cfg.language, cta_enabled=False,
    )
    dna_css = build_css_override(dna)
    template_path = current_app.config["SHORTBOT_TEMPLATES_DIR"] / f"{cfg.template}.html.j2"
    html = build_html(job, template_path,
                      ui_labels=ui_labels_for(cfg.language),
                      dna_css=dna_css)
    return Response(html, mimetype="text/html")
```

- [ ] **Step 4: Run, expect PASS**

```bash
python -m pytest tests/test_web_preview.py -v
```

- [ ] **Step 5: Commit**

```bash
git add src/short_bot/web/routes/preview.py tests/test_web_preview.py
git commit -m "feat(web): /preview/<slug> endpoint with query-param DNA overrides"
```

---

## Task 14: Threaded pipeline runner module

**Files:**
- Create: `src/short_bot/web/runs.py`
- Test: `tests/test_web_runs.py`

- [ ] **Step 1: Write failing test (`tests/test_web_runs.py`)**

```python
from unittest.mock import patch, MagicMock

from short_bot.web.runs import launch_pipeline


def test_launch_pipeline_starts_thread(tmp_path):
    fake_settings = MagicMock()
    fake_channel = MagicMock()
    fake_channel.slug = "test"

    with patch("short_bot.web.runs.run_pipeline") as m_run:
        m_run.return_value = MagicMock(run_id=42, status="success", short_path=None, error=None)
        thread = launch_pipeline(
            channel=fake_channel, settings=fake_settings,
            db_path=tmp_path / "db.sqlite",
            music_root=tmp_path / "music",
            templates_dir=tmp_path / "templates",
            cache_dir=tmp_path / "cache",
            lock_dir=tmp_path / "locks",
            logs_dir=tmp_path / "logs",
            trigger="manual",
        )
    # Thread runs in background — wait for it
    thread.join(timeout=5)
    assert m_run.called
    assert m_run.call_args.kwargs["channel"] is fake_channel
    assert m_run.call_args.kwargs["trigger"] == "manual"
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/runs.py`**

```python
"""Threaded pipeline runner — launches pipeline.run_pipeline in a daemon thread."""
import threading
from pathlib import Path

from short_bot.pipeline import run_pipeline


def launch_pipeline(*, channel, settings, db_path: Path,
                    music_root: Path, templates_dir: Path,
                    cache_dir: Path, lock_dir: Path, logs_dir: Path,
                    trigger: str = "manual") -> threading.Thread:
    """Start pipeline in a daemon thread. Returns the thread object."""
    def _runner():
        try:
            run_pipeline(
                channel=channel, settings=settings,
                db_path=db_path, music_root=music_root,
                templates_dir=templates_dir,
                cache_dir=cache_dir, lock_dir=lock_dir,
                logs_dir=logs_dir, trigger=trigger,
            )
        except Exception:
            # Already logged inside pipeline. Swallow to keep daemon thread alive.
            pass

    thread = threading.Thread(target=_runner, daemon=True)
    thread.start()
    return thread
```

- [ ] **Step 4: Run, expect PASS**

```bash
python -m pytest tests/test_web_runs.py -v
```

- [ ] **Step 5: Commit**

```bash
git add src/short_bot/web/runs.py tests/test_web_runs.py
git commit -m "feat(web): launch_pipeline helper for threaded pipeline execution"
```

---

## Task 15: "Şimdi üret" button + run status polling

**Files:**
- Modify: `src/short_bot/web/routes/shorts.py` (add "run-now" action)
- Modify: `src/short_bot/web/routes/channels.py` (add `/runs/<id>/status`)
- Create: `src/short_bot/web/templates/_partials/run_status.html.j2`
- Test: `tests/test_web_runs.py` (append)

- [ ] **Step 1: Append failing tests**

```python
def test_shorts_run_now_starts_pipeline(tmp_path, monkeypatch):
    from short_bot.web import create_app
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    (cfg_dir / "channels" / "demo.yaml").write_text(
        "slug: demo\nname: D\nlanguage: tr\nkeywords: [a]\nschedule_cron: ''\n"
        "duration_s: 6\nmin_score: 6.0\nmax_candidates_per_run: 10\ntemplate: newscast\n"
        "colors: {primary: '#000', accent: '#fff', bg_gradient: ['#000','#111']}\n"
        "handle: '@d'\noutput_dir: output/demo\nenabled: true\n"
        "cta: {enabled: false, text: '', icons: [], duration_s: 0, show_handle: false}\n",
        encoding="utf-8",
    )
    app = create_app(config_dir=cfg_dir, db_path=tmp_path / "db.sqlite", scheduler=False)

    with patch("short_bot.web.routes.shorts.launch_pipeline") as m_launch:
        m_launch.return_value = MagicMock()
        client = app.test_client()
        resp = client.post("/shorts/run-now", data={"channel": "demo"})
    assert resp.status_code in (200, 302)
    assert m_launch.called
    assert m_launch.call_args.kwargs["channel"].slug == "demo"
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Add run-now route to `src/short_bot/web/routes/shorts.py`**

Append at end of file:

```python
from short_bot.config import load_channel
from short_bot.web.runs import launch_pipeline


@bp.route("/shorts/run-now", methods=["POST"])
def run_now():
    slug = request.form.get("channel", "").strip()
    if not slug:
        abort(400)
    cfg_dir = current_app.config["SHORTBOT_CONFIG_DIR"]
    cfg_path = cfg_dir / "channels" / f"{slug}.yaml"
    if not cfg_path.exists():
        abort(404)
    cfg = load_channel(cfg_path)
    settings = current_app.config["SHORTBOT_SETTINGS"]
    launch_pipeline(
        channel=cfg, settings=settings,
        db_path=current_app.config["SHORTBOT_DB_PATH"],
        music_root=current_app.config["SHORTBOT_MUSIC_ROOT"],
        templates_dir=current_app.config["SHORTBOT_TEMPLATES_DIR"],
        cache_dir=current_app.config["SHORTBOT_CACHE_DIR"],
        lock_dir=current_app.config["SHORTBOT_LOCK_DIR"],
        logs_dir=current_app.config["SHORTBOT_LOGS_DIR"],
        trigger="manual",
    )
    return render_template("_partials/run_status.html.j2",
                            channel_slug=slug, status="running")


# Add this import at top of shorts.py:
# from flask import current_app
```

Make sure `from flask import abort, current_app, render_template, request` is at the top.

- [ ] **Step 4: Add `/runs/<run_id>/status` to channels.py**

Append:

```python
from datetime import datetime
from short_bot.web.models import Run


@bp.route("/runs/<int:run_id>/status")
def run_status(run_id):
    r = Run.query.get(run_id)
    if r is None:
        abort(404)
    return render_template("_partials/run_status.html.j2",
                            channel_slug=r.channel,
                            status=r.status, run=r)


# Add `from flask import abort, render_template`
```

- [ ] **Step 5: Implement `src/short_bot/web/templates/_partials/run_status.html.j2`**

```html
<div class="bg-slate-800 rounded p-3 text-sm" id="run-status">
  {% if status == 'running' %}
    <div class="flex items-center gap-2 text-blue-400"
         {% if run %}hx-get="/runs/{{ run.id }}/status" hx-trigger="every 2s"
                    hx-swap="outerHTML"{% endif %}>
      <span class="animate-pulse">⟳</span> {{ channel_slug }}: çalışıyor...
    </div>
  {% elif status == 'success' %}
    <div class="text-green-400">✓ {{ channel_slug }}: başarılı.
      {% if run and run.short_id %}<a href="/shorts/{{ run.short_id }}" class="underline">Yeni short</a>{% endif %}
    </div>
  {% elif status == 'failed' %}
    <div class="text-red-400">✗ {{ channel_slug }}: hata. {{ run.error if run else '' }}</div>
  {% elif status == 'no_candidates' %}
    <div class="text-yellow-400">— {{ channel_slug }}: aday yok.</div>
  {% endif %}
</div>
```

- [ ] **Step 6: Run tests, expect PASS**

```bash
python -m pytest tests/test_web_runs.py -v
```

- [ ] **Step 7: Commit**

```bash
git add src/short_bot/web/routes/shorts.py src/short_bot/web/routes/channels.py src/short_bot/web/templates/_partials/run_status.html.j2 tests/test_web_runs.py
git commit -m "feat(web): 'Şimdi üret' button + HTMX run-status polling"
```

---

## Task 16: Channel new wizard (form + generate)

**Files:**
- Modify: `src/short_bot/web/routes/channel_new.py`
- Create: `src/short_bot/web/templates/channels/new.html.j2`
- Create: `src/short_bot/web/templates/_partials/dna_preview.html.j2`
- Test: `tests/test_web_channel_new.py`

- [ ] **Step 1: Write failing test (`tests/test_web_channel_new.py`)**

```python
import pytest
from unittest.mock import patch

from short_bot.dna import DnaSpec, DnaPalette, DnaFonts, DnaTone
from short_bot.web import create_app


@pytest.fixture
def app(tmp_path):
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "settings.yaml").write_text(
        "ffmpeg_path: ffmpeg\nclaude_cli_path: claude\nplaywright_browser: chromium\n"
        "web: {host: 127.0.0.1, port: 5005}\n"
        "fuzzy_dedup_threshold: 0.85\nlog_level: INFO\n"
        "claude_models:\n  dna: opus\n  default: haiku\n",
        encoding="utf-8",
    )
    return create_app(config_dir=cfg_dir, db_path=tmp_path / "x.sqlite", scheduler=False)


def _fake_dna():
    return DnaSpec(
        archetype="newscast",
        palette=DnaPalette(primary="#c81e1e", accent="#ffea3b",
                           bg_gradient=["#1a3b6b","#0a1a3b"],
                           body_bg=["#1a1a2a","#0a0a1a"]),
        fonts=DnaFonts(headline="Inter", body="Inter"),
        tone=DnaTone(voice="x", style="y"),
        persona_summary="z",
    )


def test_new_channel_wizard_form_renders(app):
    client = app.test_client()
    resp = client.get("/channels/new")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "İsim" in body or "Name" in body or "name" in body.lower()


def test_generate_dna_returns_partial(app):
    client = app.test_client()
    with patch("short_bot.web.routes.channel_new.generate_dna", return_value=_fake_dna()):
        resp = client.post("/channels/new/generate", data={
            "name": "Test Kanal", "language": "tr",
            "keywords": "a,b,c", "topic_hint": "", "target_audience": "",
        })
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "newscast" in body
    assert "#c81e1e" in body


def test_save_creates_channel_yaml_and_css(app):
    client = app.test_client()
    with patch("short_bot.web.routes.channel_new.generate_dna", return_value=_fake_dna()):
        # First generate (puts DNA in session)
        gen_resp = client.post("/channels/new/generate", data={
            "name": "Test Kanal", "language": "tr",
            "keywords": "a,b,c", "topic_hint": "", "target_audience": "",
        })
        # Then save
        save_resp = client.post("/channels/new/save", data={
            "name": "Test Kanal", "language": "tr",
            "keywords": "a,b,c",
        })
    assert save_resp.status_code in (200, 302)
    cfg_dir = app.config["SHORTBOT_CONFIG_DIR"]
    yaml_path = cfg_dir / "channels" / "test-kanal.yaml"
    assert yaml_path.exists()
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/routes/channel_new.py`**

```python
import json
import re
import unicodedata
from pathlib import Path

from flask import (Blueprint, abort, current_app, redirect, render_template,
                    request, session, url_for)

from short_bot.config import (ChannelConfig, save_channel)
from short_bot.dna import DnaSpec, build_css_override, generate_dna
from short_bot.locale import RSS_LOCALES, SUPPORTED_LANGUAGES

bp = Blueprint("channel_new", __name__)


def _slug_from_name(name: str) -> str:
    name = unicodedata.normalize("NFKD", name)
    name = "".join(c for c in name if not unicodedata.combining(c))
    name = re.sub(r"[^a-zA-Z0-9]+", "-", name).strip("-").lower()
    return name or "channel"


@bp.route("/channels/new")
def form():
    return render_template("channels/new.html.j2")


@bp.route("/channels/new/generate", methods=["POST"])
def generate():
    name = request.form.get("name", "").strip()
    language = request.form.get("language", "tr").strip()
    if language not in SUPPORTED_LANGUAGES:
        abort(400)
    keywords = [k.strip() for k in request.form.get("keywords", "").split(",") if k.strip()]
    settings = current_app.config["SHORTBOT_SETTINGS"]
    try:
        dna = generate_dna(
            name=name, keywords=keywords, language=language,
            topic_hint=request.form.get("topic_hint", ""),
            target_audience=request.form.get("target_audience", ""),
            claude_path=settings.claude_cli_path,
            model=settings.claude_models.get("dna", "opus"),
        )
    except Exception as e:
        return render_template("_partials/dna_preview.html.j2",
                                error=str(e), dna=None, name=name, language=language)
    session["wizard_dna"] = dna.model_dump_json()
    session["wizard_name"] = name
    session["wizard_language"] = language
    return render_template("_partials/dna_preview.html.j2",
                            error=None, dna=dna, name=name, language=language)


@bp.route("/channels/new/save", methods=["POST"])
def save():
    if "wizard_dna" not in session:
        abort(400)
    dna = DnaSpec.model_validate_json(session["wizard_dna"])
    name = session.get("wizard_name", request.form.get("name", "Channel"))
    language = session.get("wizard_language", request.form.get("language", "tr"))
    keywords = [k.strip() for k in request.form.get("keywords", "").split(",") if k.strip()]
    slug = _slug_from_name(name)

    cfg_dir = current_app.config["SHORTBOT_CONFIG_DIR"]
    templates_dir = current_app.config["SHORTBOT_TEMPLATES_DIR"]
    yaml_path = cfg_dir / "channels" / f"{slug}.yaml"
    css_path = templates_dir / "css" / f"{slug}.css"
    css_path.parent.mkdir(parents=True, exist_ok=True)
    css_path.write_text(build_css_override(dna), encoding="utf-8")

    cfg = ChannelConfig(
        slug=slug, name=name, keywords=keywords,
        rss_locale=RSS_LOCALES[language],
        schedule_cron="0 8,14,20 * * *",
        duration_s=6, min_score=6.0, max_candidates_per_run=10,
        template=dna.archetype,
        colors={"primary": dna.palette.primary,
                 "accent": dna.palette.accent,
                 "bg_gradient": dna.palette.bg_gradient},
        handle=f"@{slug}", output_dir=f"output/{slug}",
        enabled=True,
        cta_enabled=False, cta_text="", cta_icons=[],
        cta_duration_s=0, cta_show_handle=False,
        language=language, dna=dna, script_model=None,
    )
    save_channel(yaml_path, cfg)
    session.pop("wizard_dna", None)
    session.pop("wizard_name", None)
    session.pop("wizard_language", None)
    return redirect(url_for("channel_edit.edit", slug=slug))
```

- [ ] **Step 4: Implement `src/short_bot/web/templates/channels/new.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — Yeni Kanal{% endblock %}
{% block content %}
<a href="/channels" class="text-slate-400 hover:text-white text-sm">← Kanallar</a>
<h1 class="text-3xl font-bold mt-2 mb-6">+ Yeni Kanal</h1>

<div class="grid grid-cols-2 gap-6">
  <form id="wizard-form" hx-post="/channels/new/generate" hx-target="#preview" hx-indicator="#loading"
        class="space-y-3">
    <h2 class="text-lg font-bold">Temel Bilgi</h2>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Kanal adı</span>
      <input type="text" name="name" required
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Dil</span>
      <select name="language" class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
        <option value="tr">🇹🇷 Türkçe</option>
        <option value="en">🇬🇧 English</option>
        <option value="de">🇩🇪 Deutsch</option>
        <option value="es">🇪🇸 Español</option>
        <option value="fr">🇫🇷 Français</option>
      </select>
    </label>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Keywords (virgülle)</span>
      <input type="text" name="keywords" required
             placeholder="Bundesliga, Bayern, Champions League"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Konu ipucu (opsiyonel)</span>
      <input type="text" name="topic_hint"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Hedef kitle (opsiyonel)</span>
      <input type="text" name="target_audience"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>

    <button type="submit" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded font-semibold mt-4 w-full">
      ⚡ DNA Üret (Opus, ~30-60s)
    </button>
    <div id="loading" class="htmx-indicator text-blue-400 text-sm">
      ⟳ Opus düşünüyor...
    </div>
  </form>

  <div>
    <h2 class="text-lg font-bold mb-3">Canlı Önizleme</h2>
    <div id="preview" class="bg-slate-950 rounded-lg p-4 text-slate-500 text-center text-sm aspect-[9/16] flex items-center justify-center">
      Önce DNA üret →
    </div>
  </div>
</div>
{% endblock %}
```

- [ ] **Step 5: Implement `src/short_bot/web/templates/_partials/dna_preview.html.j2`**

```html
{% if error %}
  <div class="bg-red-900/50 border border-red-700 rounded p-4 text-red-300 text-sm">
    ✗ Hata: {{ error }}
  </div>
{% elif dna %}
  <div class="bg-slate-800 rounded p-3 text-xs space-y-2 mb-3">
    <div><span class="text-slate-400">Archetype:</span> <strong>{{ dna.archetype }}</strong></div>
    <div><span class="text-slate-400">Palette:</span>
      <span class="inline-block w-4 h-4 rounded align-middle ml-1" style="background: {{ dna.palette.primary }}"></span> {{ dna.palette.primary }}
      <span class="inline-block w-4 h-4 rounded align-middle ml-2" style="background: {{ dna.palette.accent }}"></span> {{ dna.palette.accent }}
    </div>
    <div><span class="text-slate-400">Fonts:</span> {{ dna.fonts.headline }} + {{ dna.fonts.body }}</div>
  </div>
  <div class="flex gap-2 mt-2">
    <form hx-post="/channels/new/generate" hx-target="#preview" hx-include="#wizard-form" class="flex-1">
      <button class="bg-slate-700 hover:bg-slate-600 px-3 py-2 rounded text-sm w-full">↻ Yeniden Üret</button>
    </form>
    <form action="/channels/new/save" method="POST" hx-include="#wizard-form" class="flex-1">
      <input type="hidden" name="name" value="{{ name }}">
      <input type="hidden" name="language" value="{{ language }}">
      <button class="bg-red-600 hover:bg-red-700 px-3 py-2 rounded text-sm w-full font-semibold">💾 Kaydet</button>
    </form>
  </div>
{% endif %}
```

- [ ] **Step 6: Run, expect PASS**

```bash
python -m pytest tests/test_web_channel_new.py -v
```

- [ ] **Step 7: Commit**

```bash
git add src/short_bot/web/routes/channel_new.py src/short_bot/web/templates/channels/new.html.j2 src/short_bot/web/templates/_partials/dna_preview.html.j2 tests/test_web_channel_new.py
git commit -m "feat(web): channel creation wizard (form + generate + save)"
```

---

## Task 17: APScheduler integration

**Files:**
- Create: `src/short_bot/web/scheduler.py`
- Test: `tests/test_web_scheduler.py`

- [ ] **Step 1: Write failing test (`tests/test_web_scheduler.py`)**

```python
from unittest.mock import patch, MagicMock


def test_init_scheduler_registers_jobs(tmp_path):
    from flask import Flask
    from short_bot.web.scheduler import init_scheduler

    app = Flask(__name__)
    cfg_dir = tmp_path / "config"
    (cfg_dir / "channels").mkdir(parents=True)
    (cfg_dir / "channels" / "demo.yaml").write_text(
        "slug: demo\nname: D\nlanguage: tr\nkeywords: [a]\n"
        "schedule_cron: '0 * * * *'\nduration_s: 6\nmin_score: 6.0\n"
        "max_candidates_per_run: 10\ntemplate: newscast\n"
        "colors: {primary: '#000', accent: '#fff', bg_gradient: ['#000','#111']}\n"
        "handle: '@d'\noutput_dir: output/demo\nenabled: true\n"
        "cta: {enabled: false, text: '', icons: [], duration_s: 0, show_handle: false}\n",
        encoding="utf-8",
    )
    app.config["SHORTBOT_CONFIG_DIR"] = cfg_dir
    app.config["SHORTBOT_DB_PATH"] = tmp_path / "db.sqlite"
    app.config["SHORTBOT_MUSIC_ROOT"] = tmp_path / "music"
    app.config["SHORTBOT_TEMPLATES_DIR"] = tmp_path / "templates"
    app.config["SHORTBOT_CACHE_DIR"] = tmp_path / "cache"
    app.config["SHORTBOT_LOCK_DIR"] = tmp_path / "locks"
    app.config["SHORTBOT_LOGS_DIR"] = tmp_path / "logs"
    app.config["SHORTBOT_SETTINGS"] = MagicMock()

    with patch("short_bot.web.scheduler.BackgroundScheduler") as m_sched:
        instance = MagicMock()
        m_sched.return_value = instance
        scheduler = init_scheduler(app)
    # Should have called add_job at least once for demo channel
    assert instance.add_job.called
    # And once for the reload-jobs interval job
    job_ids = [call.kwargs.get("id") for call in instance.add_job.call_args_list]
    assert "demo" in job_ids
    assert "_reload_jobs" in job_ids
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement `src/short_bot/web/scheduler.py`**

```python
"""APScheduler wiring — registers cron jobs from channel YAMLs."""
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger

from short_bot.config import list_channels, load_channel


def init_scheduler(app):
    scheduler = BackgroundScheduler()

    def _run_for_channel(slug: str):
        from short_bot.web.runs import launch_pipeline
        cfg_dir = app.config["SHORTBOT_CONFIG_DIR"]
        cfg = load_channel(cfg_dir / "channels" / f"{slug}.yaml")
        settings = app.config["SHORTBOT_SETTINGS"]
        launch_pipeline(
            channel=cfg, settings=settings,
            db_path=app.config["SHORTBOT_DB_PATH"],
            music_root=app.config["SHORTBOT_MUSIC_ROOT"],
            templates_dir=app.config["SHORTBOT_TEMPLATES_DIR"],
            cache_dir=app.config["SHORTBOT_CACHE_DIR"],
            lock_dir=app.config["SHORTBOT_LOCK_DIR"],
            logs_dir=app.config["SHORTBOT_LOGS_DIR"],
            trigger="cron",
        )

    def _reload_jobs():
        cfg_dir = app.config["SHORTBOT_CONFIG_DIR"]
        # Remove all per-channel jobs (keep _reload_jobs)
        for job in list(scheduler.get_jobs()):
            if job.id != "_reload_jobs":
                scheduler.remove_job(job.id)
        for cfg in list_channels(cfg_dir / "channels", enabled_only=True):
            if not cfg.schedule_cron:
                continue
            try:
                trigger = CronTrigger.from_crontab(cfg.schedule_cron)
            except ValueError:
                continue  # invalid cron, skip
            scheduler.add_job(
                _run_for_channel, trigger,
                args=[cfg.slug], id=cfg.slug,
                max_instances=1, replace_existing=True,
            )

    # Initial register
    _reload_jobs()
    # Re-scan channel configs every 5 minutes
    scheduler.add_job(_reload_jobs, "interval", minutes=5, id="_reload_jobs")

    scheduler.start()
    app.scheduler = scheduler
    return scheduler
```

- [ ] **Step 4: Run, expect PASS**

```bash
python -m pytest tests/test_web_scheduler.py -v
```

- [ ] **Step 5: Commit**

```bash
git add src/short_bot/web/scheduler.py tests/test_web_scheduler.py
git commit -m "feat(web): APScheduler with cron jobs + 5-min config reload"
```

---

## Task 18: DNA editor UI on channel edit page (palette/fonts/segments)

**Files:**
- Modify: `src/short_bot/web/templates/channels/edit.html.j2` (extend with DNA editor section)
- Modify: `src/short_bot/web/routes/channel_edit.py` (handle DNA fields in save)
- Test: `tests/test_web_channels.py` (append)

- [ ] **Step 1: Append failing tests**

```python
def test_channel_edit_renders_dna_editor_when_dna_present(app, tmp_path):
    # Add a channel with DNA
    cfg_dir = app.config["SHORTBOT_CONFIG_DIR"]
    (cfg_dir / "channels" / "with-dna.yaml").write_text(
        """slug: with-dna
name: With DNA
language: tr
keywords: [a]
schedule_cron: '0 * * * *'
duration_s: 6
min_score: 6.0
max_candidates_per_run: 10
template: stadium
colors: {primary: '#0a4d2a', accent: '#ffd700', bg_gradient: ['#1a8b3a','#0a4d1a']}
handle: '@x'
output_dir: output/with-dna
enabled: true
cta: {enabled: false, text: '', icons: [], duration_s: 0, show_handle: false}
dna:
  archetype: stadium
  palette:
    primary: '#0a4d2a'
    accent: '#ffd700'
    bg_gradient: ['#1a8b3a', '#0a4d1a']
    body_bg: ['#0a1a0a', '#000000']
  fonts: {headline: Oswald, body: Inter, google_imports: []}
  tone: {voice: x, style: y, forbidden: [], sentence_max_words: 14, paragraph_sentences: [3, 4], body_max_chars: 280, headline_style_hint: ''}
  banner_shape: slanted
  highlight_style: marker
  chip_style: pill
  category_icon: '⚽'
  search_query_template: '{header_top}'
  persona_summary: ''
""", encoding="utf-8")
    client = app.test_client()
    resp = client.get("/channels/with-dna/edit")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert 'type="color"' in body
    assert "Oswald" in body  # font dropdown current value
    assert "slanted" in body  # banner shape


def test_channel_edit_saves_dna_palette_change(app, tmp_path):
    cfg_dir = app.config["SHORTBOT_CONFIG_DIR"]
    yaml_path = cfg_dir / "channels" / "demo-tr.yaml"
    # Add DNA to demo-tr first
    yaml_path.write_text(
        """slug: demo-tr
name: Demo TR
language: tr
keywords: [a]
schedule_cron: '0 * * * *'
duration_s: 6
min_score: 6.0
max_candidates_per_run: 10
template: newscast
colors: {primary: '#c81e1e', accent: '#ffea3b', bg_gradient: ['#1a3b6b','#0a1a3b']}
handle: '@demo'
output_dir: output/demo
enabled: true
cta: {enabled: false, text: '', icons: [], duration_s: 0, show_handle: false}
dna:
  archetype: newscast
  palette:
    primary: '#c81e1e'
    accent: '#ffea3b'
    bg_gradient: ['#1a3b6b', '#0a1a3b']
    body_bg: ['#1a1a2a', '#0a0a1a']
  fonts: {headline: Inter, body: Inter, google_imports: []}
  tone: {voice: x, style: y, forbidden: [], sentence_max_words: 14, paragraph_sentences: [3, 5], body_max_chars: 350, headline_style_hint: ''}
  banner_shape: flat
  highlight_style: bg-flat
  chip_style: rounded
  category_icon: ''
  search_query_template: '{header_top}'
  persona_summary: ''
""", encoding="utf-8")
    client = app.test_client()
    client.post("/channels/demo-tr/edit", data={
        "keywords": "a", "schedule_cron": "0 * * * *",
        "handle": "@demo", "duration_s": "6", "min_score": "6.0", "enabled": "1",
        "dna_primary": "#ff0000", "dna_accent": "#00ff00",
        "dna_font_headline": "Bebas Neue", "dna_font_body": "Roboto",
        "dna_banner_shape": "ribbon", "dna_highlight_style": "marker",
        "dna_chip_style": "pill", "dna_category_icon": "🎬",
    })
    from short_bot.config import load_channel
    cfg = load_channel(yaml_path)
    assert cfg.dna.palette.primary == "#ff0000"
    assert cfg.dna.palette.accent == "#00ff00"
    assert cfg.dna.fonts.headline == "Bebas Neue"
    assert cfg.dna.banner_shape == "ribbon"
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Replace `src/short_bot/web/templates/channels/edit.html.j2`**

```html
{% extends "base.html.j2" %}
{% block title %}short-bot — {{ c.slug }}{% endblock %}
{% block content %}
<a href="/channels" class="text-slate-400 hover:text-white text-sm">← Kanallar</a>
<h1 class="text-3xl font-bold mt-2 mb-1">{{ c.name }}</h1>
<div class="text-slate-400 text-sm mb-6">
  {{ c.slug }} · {{ c.language }} · {{ c.template }}
  {% if c.dna %}<span class="bg-green-700 text-white px-2 py-0.5 rounded text-xs ml-2">DNA</span>{% endif %}
</div>

<form method="POST" class="grid grid-cols-2 gap-6"
      x-data='{ primary: "{{ c.dna.palette.primary if c.dna else c.colors.primary }}",
                accent: "{{ c.dna.palette.accent if c.dna else c.colors.accent }}",
                fontHeadline: "{{ c.dna.fonts.headline if c.dna else "Inter" }}" }'>
  <div class="space-y-3">
    <h2 class="text-lg font-bold">Temel</h2>
    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Keywords</span>
      <input type="text" name="keywords" value="{{ c.keywords | join(', ') }}"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>
    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Cron</span>
      <input type="text" name="schedule_cron" value="{{ c.schedule_cron }}"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 font-mono text-sm">
    </label>
    <div class="grid grid-cols-2 gap-3">
      <label class="block">
        <span class="text-xs text-slate-400 uppercase">Süre (sn)</span>
        <input type="number" name="duration_s" value="{{ c.duration_s }}"
               class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
      </label>
      <label class="block">
        <span class="text-xs text-slate-400 uppercase">Min skor</span>
        <input type="number" step="0.1" name="min_score" value="{{ c.min_score }}"
               class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
      </label>
    </div>
    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Handle</span>
      <input type="text" name="handle" value="{{ c.handle }}"
             class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
    </label>
    <label class="flex items-center gap-2">
      <input type="checkbox" name="enabled" value="1" {% if c.enabled %}checked{% endif %}>
      <span>Aktif</span>
    </label>

    {% if c.dna %}
    <h2 class="text-lg font-bold mt-6 border-t border-slate-700 pt-4">DNA Editör</h2>

    <div class="grid grid-cols-2 gap-3">
      <label class="block">
        <span class="text-xs text-slate-400 uppercase">Primary</span>
        <input type="color" name="dna_primary" x-model="primary"
               class="bg-slate-800 rounded w-full mt-1 h-9">
      </label>
      <label class="block">
        <span class="text-xs text-slate-400 uppercase">Accent</span>
        <input type="color" name="dna_accent" x-model="accent"
               class="bg-slate-800 rounded w-full mt-1 h-9">
      </label>
    </div>

    <div class="grid grid-cols-2 gap-3">
      <label class="block">
        <span class="text-xs text-slate-400 uppercase">Headline font</span>
        <select name="dna_font_headline" x-model="fontHeadline"
                class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
          {% for f in ["Inter", "Bebas Neue", "Playfair Display", "Anton", "JetBrains Mono", "Oswald", "Impact", "Roboto", "Montserrat", "Lato", "Open Sans"] %}
            <option value="{{ f }}" {% if c.dna.fonts.headline == f %}selected{% endif %}>{{ f }}</option>
          {% endfor %}
        </select>
      </label>
      <label class="block">
        <span class="text-xs text-slate-400 uppercase">Body font</span>
        <select name="dna_font_body" class="bg-slate-800 px-3 py-2 rounded w-full mt-1 text-sm">
          {% for f in ["Inter", "Roboto", "Lato", "Open Sans", "Montserrat", "Georgia"] %}
            <option value="{{ f }}" {% if c.dna.fonts.body == f %}selected{% endif %}>{{ f }}</option>
          {% endfor %}
        </select>
      </label>
    </div>

    <div>
      <span class="text-xs text-slate-400 uppercase">Banner shape</span>
      <div class="flex gap-2 mt-1">
        {% for s in ["flat", "ribbon", "slanted", "sharp"] %}
          <label class="flex-1">
            <input type="radio" name="dna_banner_shape" value="{{ s }}"
                   {% if c.dna.banner_shape == s %}checked{% endif %} class="hidden peer">
            <div class="bg-slate-800 hover:bg-slate-700 peer-checked:bg-red-600 px-3 py-2 rounded text-center text-sm cursor-pointer">{{ s }}</div>
          </label>
        {% endfor %}
      </div>
    </div>

    <div>
      <span class="text-xs text-slate-400 uppercase">Highlight style</span>
      <div class="flex gap-2 mt-1">
        {% for s in ["bg-flat", "underline", "marker", "neon"] %}
          <label class="flex-1">
            <input type="radio" name="dna_highlight_style" value="{{ s }}"
                   {% if c.dna.highlight_style == s %}checked{% endif %} class="hidden peer">
            <div class="bg-slate-800 hover:bg-slate-700 peer-checked:bg-red-600 px-3 py-2 rounded text-center text-sm cursor-pointer">{{ s }}</div>
          </label>
        {% endfor %}
      </div>
    </div>

    <div>
      <span class="text-xs text-slate-400 uppercase">Chip style</span>
      <div class="flex gap-2 mt-1">
        {% for s in ["rounded", "sharp", "pill"] %}
          <label class="flex-1">
            <input type="radio" name="dna_chip_style" value="{{ s }}"
                   {% if c.dna.chip_style == s %}checked{% endif %} class="hidden peer">
            <div class="bg-slate-800 hover:bg-slate-700 peer-checked:bg-red-600 px-3 py-2 rounded text-center text-sm cursor-pointer">{{ s }}</div>
          </label>
        {% endfor %}
      </div>
    </div>

    <label class="block">
      <span class="text-xs text-slate-400 uppercase">Category icon</span>
      <input type="text" name="dna_category_icon" value="{{ c.dna.category_icon }}"
             maxlength="4" class="bg-slate-800 px-3 py-2 rounded w-24 mt-1 text-2xl text-center">
    </label>
    {% endif %}

    <button type="submit" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded font-semibold mt-6">
      💾 Kaydet
    </button>
  </div>

  <div>
    <h2 class="text-lg font-bold mb-3">Önizleme</h2>
    <iframe x-bind:src="`/preview/{{ c.slug }}?primary=${encodeURIComponent(primary)}&accent=${encodeURIComponent(accent)}&font_headline=${encodeURIComponent(fontHeadline)}`"
            src="/preview/{{ c.slug }}"
            class="w-full bg-black rounded"
            style="aspect-ratio: 9/16; max-height: 700px;"></iframe>
  </div>
</form>
{% endblock %}
```

- [ ] **Step 4: Update `save` in `channel_edit.py` to handle DNA fields**

Replace the body of the `save()` POST handler with:

```python
@bp.route("/channels/<slug>/edit", methods=["POST"])
def save(slug):
    path = _yaml_path(slug)
    if not path.exists():
        abort(404)
    cfg = load_channel(path)

    keywords = [k.strip() for k in request.form.get("keywords", "").split(",") if k.strip()]

    new_dna = cfg.dna
    if cfg.dna and request.form.get("dna_primary"):
        # Apply DNA tweaks from form
        from short_bot.dna import build_css_override
        new_dna = cfg.dna.model_copy(update={
            "palette": cfg.dna.palette.model_copy(update={
                "primary": request.form.get("dna_primary", cfg.dna.palette.primary),
                "accent": request.form.get("dna_accent", cfg.dna.palette.accent),
            }),
            "fonts": cfg.dna.fonts.model_copy(update={
                "headline": request.form.get("dna_font_headline", cfg.dna.fonts.headline),
                "body": request.form.get("dna_font_body", cfg.dna.fonts.body),
            }),
            "banner_shape": request.form.get("dna_banner_shape", cfg.dna.banner_shape),
            "highlight_style": request.form.get("dna_highlight_style", cfg.dna.highlight_style),
            "chip_style": request.form.get("dna_chip_style", cfg.dna.chip_style),
            "category_icon": request.form.get("dna_category_icon", cfg.dna.category_icon),
        })
        # Rebuild CSS
        templates_dir = current_app.config["SHORTBOT_TEMPLATES_DIR"]
        css_path = templates_dir / "css" / f"{slug}.css"
        css_path.parent.mkdir(parents=True, exist_ok=True)
        css_path.write_text(build_css_override(new_dna), encoding="utf-8")

    new_cfg = ChannelConfig(
        slug=cfg.slug, name=cfg.name, keywords=keywords,
        rss_locale=cfg.rss_locale,
        schedule_cron=request.form.get("schedule_cron", cfg.schedule_cron),
        duration_s=int(request.form.get("duration_s", cfg.duration_s)),
        min_score=float(request.form.get("min_score", cfg.min_score)),
        max_candidates_per_run=cfg.max_candidates_per_run,
        template=cfg.template,
        colors={"primary": new_dna.palette.primary if new_dna else cfg.colors["primary"],
                 "accent": new_dna.palette.accent if new_dna else cfg.colors["accent"],
                 "bg_gradient": cfg.colors["bg_gradient"]},
        handle=request.form.get("handle", cfg.handle),
        output_dir=cfg.output_dir,
        enabled=request.form.get("enabled") == "1",
        cta_enabled=cfg.cta_enabled, cta_text=cfg.cta_text,
        cta_icons=cfg.cta_icons, cta_duration_s=cfg.cta_duration_s,
        cta_show_handle=cfg.cta_show_handle,
        language=cfg.language, dna=new_dna, script_model=cfg.script_model,
    )
    save_channel(path, new_cfg)
    return redirect(url_for("channel_edit.edit", slug=slug))
```

- [ ] **Step 5: Run, expect PASS**

```bash
python -m pytest tests/test_web_channels.py -v
```

- [ ] **Step 6: Commit**

```bash
git add src/short_bot/web/routes/channel_edit.py src/short_bot/web/templates/channels/edit.html.j2 tests/test_web_channels.py
git commit -m "feat(web): DNA editor on channel edit (palette/fonts/banner/highlight/chip)"
```

---

## Task 19: Final integration + manual smoke

**Files:**
- Modify: `README.md` (add panel section)
- Manual smoke (no automated test)

- [ ] **Step 1: Run full test suite**

```bash
python -m pytest -v
```
Expected: 150+41 ≈ 191 passing.

- [ ] **Step 2: Manual smoke**

```bash
python -m short_bot web
```

Expected stdout:
```
Web panel: http://127.0.0.1:5005
Scheduler: see /channels
Press Ctrl+C to stop.
```

In browser open http://127.0.0.1:5005:
- [ ] Dashboard loads, shows current 2 channels (or more if added)
- [ ] /shorts shows produced mp4 grid; click → detail page plays mp4
- [ ] /rss shows scoring history
- [ ] /channels shows son-dakika + spor-short-de + DNA badge for spor-short-de
- [ ] /channels/spor-short-de/edit: tweak primary color → iframe preview updates instantly → Kaydet → page reloads with new palette
- [ ] /channels/new: form → fill EN tabloid magazin → Generate (Opus ~30-60s) → preview iframe shows new tabloid → Save → redirected to edit
- [ ] /logs: tail polls every 2s

- [ ] **Step 3: Update README.md**

Append to `README.md`:

```markdown
## Web Panel (Phase 3)

```bash
python -m short_bot web
# → http://127.0.0.1:5005
```

Features:
- Dashboard with daily counters and recent shorts
- Shorts gallery (filter by channel/search) + detail player
- RSS scoring history (last 24h)
- Channel list + edit (DNA editor with color pickers, font dropdowns, segment pills)
- New-channel wizard (form → Opus DNA → live preview → save)
- Live template preview in iframe (instant updates as you tweak DNA)
- APScheduler runs cron jobs in-process (kanal config'lerinden okur)
- Logs tail with HTMX 2s polling

Default port: 5005. Override with `--port` flag.
```

- [ ] **Step 4: Commit + tag**

```bash
git add README.md
git commit -m "docs: web panel section in README"
git tag -a v0.3.0-web-panel -m "Phase 3 complete: Flask + HTMX panel"
```

---

## Phase 3 Complete

**What works after this plan:**
- `python -m short_bot web` → http://127.0.0.1:5005 → full management UI
- Dashboard, Shorts gallery, RSS table, Channel list with DNA editor, new-channel wizard, logs tail
- APScheduler runs cron from channel YAMLs (BackgroundScheduler in-process)
- Live preview via `/preview/<slug>` iframe — color pickers update preview instantly via Alpine.js + query params
- All Phase 1+2 CLI commands still work alongside

**Out of scope (future phases):**
- Auth (basic auth / OAuth)
- Multi-user
- WebSocket (HTMX polling sufficient)
- Mobile responsive
- Headless scheduler mode (web-less)
- YouTube auto-upload
- Backup/restore
